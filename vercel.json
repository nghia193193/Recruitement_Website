{
    "version": 2,
    "builds": [
        {
            "src": "api/v1/app.js",
            "use": "@vercel/node"
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "api/v1/app.js",
            "methods": ["GET", "POST", "PUT", "PATCH", "DELETE"]
        }
    ]
}